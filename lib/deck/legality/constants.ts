/** Exact card count required for Commander (and Oathbreaker) decks. */
export const COMMANDER_DECK_SIZE = 100;

/** Minimum mainboard card count for sixty-card formats. */
export const SIXTY_CARD_MIN = 60;

/** Maximum sideboard card count for sixty-card formats. */
export const SIDEBOARD_MAX = 15;
